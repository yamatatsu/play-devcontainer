{
  "name": "play-dev-container-nx-hono",
  "license": "MIT",
  "scripts": {
    "format": "biome format --write",
    "check": "concurrently 'npm:check:*'",
    "check:biome": "biome check .",
    "check:spell": "cspell lint . --gitignore --report=simple --dot",
    "fix:biome": "biome format . --write"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "concurrently": "^9.2.1",
    "cspell": "9.2.1"
  }
}
